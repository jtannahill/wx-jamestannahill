---
interface Props {
  title: string;
  description: string;
  canonical: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
}

const { title, description, canonical, ogTitle, ogDescription, ogImage } = Astro.props;
const og = {
  title: ogTitle ?? title,
  description: ogDescription ?? description,
  image: ogImage ?? 'https://wx.jamestannahill.com/og.png',
};
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="James Tannahill" />
    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#0a0a0a" />
    <link rel="canonical" href={canonical} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" sizes="48x48" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

    <meta property="og:site_name" content="wx.jamestannahill.com" />
    <meta property="og:title" content={og.title} />
    <meta property="og:description" content={og.description} />
    <meta property="og:image" content={og.image} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:alt" content="wx.jamestannahill.com — Live Midtown Manhattan Weather" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content="en_US" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={og.title} />
    <meta name="twitter:description" content={og.description} />
    <meta name="twitter:image" content={og.image} />
    <meta name="twitter:image:alt" content="wx.jamestannahill.com — Live Midtown Manhattan Weather" />
    <meta name="twitter:creator" content="@jamestannahill" />

    <link rel="sitemap" type="application/xml" href="/sitemap.xml" />
    <link rel="ai" href="/llms.txt" />

    <link rel="preconnect" href="https://fonts.jamestannahill.com" crossorigin />
    <link rel="preconnect" href="https://api.wx.jamestannahill.com" crossorigin />

    <link rel="stylesheet" href="https://fonts.jamestannahill.com/nhg-display.css" />
    <link rel="stylesheet" href="/style.css" />

    <slot name="schema" />

    <script type="text/partytown" async src="https://www.googletagmanager.com/gtag/js?id=G-WRDEHD4QYL"></script>
    <script type="text/partytown">
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-WRDEHD4QYL');
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
